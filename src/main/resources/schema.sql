
-- TRUNCATE TABLE band RESTART IDENTITY CASCADE;
-- TRUNCATE TABLE language_texts RESTART IDENTITY CASCADE;
--  TRUNCATE TABLE genres RESTART IDENTITY CASCADE;
-- TRUNCATE TABLE countrys RESTART IDENTITY CASCADE;
-- TRUNCATE TABLE song RESTART IDENTITY CASCADE;

CREATE TABLE IF NOT EXISTS band (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY,
    name TEXT NOT NULL,
    CONSTRAINT pk_band PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS language_texts
(
    band_id BIGINT NOT NULL,
    languages TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS genres
(
    band_id BIGINT NOT NULL,
    genres TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS countrys
(
    band_id BIGINT NOT NULL,
    countrys TEXT NOT NULL
);

/*ALTER TABLE genres
    ADD CONSTRAINT fk_genre_on_band FOREIGN KEY (band_id) REFERENCES band (id);

ALTER TABLE countrys
    ADD CONSTRAINT fk_country_on_band FOREIGN KEY (band_id) REFERENCES band (id);*/

CREATE TABLE IF NOT EXISTS song (
    song_id BIGINT GENERATED BY DEFAULT AS IDENTITY,
    band_id BIGINT,
    name_song TEXT NOT NULL,
    text_song TEXT,
    release_date DATE,
CONSTRAINT pk_song PRIMARY KEY (song_id)
    );

